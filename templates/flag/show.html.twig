{% extends 'base.html.twig' %}

{% block title %}Flag{% endblock %}

{% block content %}
    <h1>Flag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flag.id }}</td>
            </tr>
            <tr>
                <th>Nama</th>
                <td>{{ flag.nama }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('app_flag_index') }}">Back</a>

    <a class="btn btn-success" href="{{ path('app_flag_edit', {'id': flag.id}) }}">Edit</a>

    {{ include('flag/_delete_form.html.twig') }}
{% endblock %}
