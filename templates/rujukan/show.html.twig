{% extends 'base.html.twig' %}

{% block title %}Rujukan{% endblock %}

{% block content %}
    <h1>Rujukan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rujukan.id }}</td>
            </tr>
            <tr>
                <th>Nama</th>
                <td>{{ rujukan.nama }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_rujukan_index') }}">Back</a>

    <a class="btn btn-primary" href="{{ path('app_rujukan_edit', {'id': rujukan.id}) }}">Edit</a>

    {{ include('rujukan/_delete_form.html.twig') }}
{% endblock %}
