{% extends 'base.html.twig' %}

{% block title %}Edit Rujukan{% endblock %}

{% block content %}
    <h1>Edit Rujukan</h1>

    {{ include('rujukan/_form.html.twig', {'button_label': 'Update'}) }}
<br>
    <a class="btn btn-primary" href="{{ path('app_rujukan_index') }}">Back</a>

    {{ include('rujukan/_delete_form.html.twig') }}
{% endblock %}
