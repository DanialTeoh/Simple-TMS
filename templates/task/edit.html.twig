{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block content %}
    <h1>Edit Task</h1>

    {{ include('task/_form.html.twig', {'button_label': 'Update'}) }}
    <br>

    <a href="{{ path('app_task_index') }}" class="btn btn-success" >back to list</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
